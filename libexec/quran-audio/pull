#!/usr/bin/env ruby
# frozen_string_literal: true

require "quran/audio"
def main(argv)
  Quran::Audio::Command::Pull
    .new(argv)
    .run
end
interrupt = catch(:interrupt) { main(ARGV) }
exit(1) if interrupt
